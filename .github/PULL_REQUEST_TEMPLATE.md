## What does this PR do?

<!-- one or two sentences -->

## Screenshots

<!-- UI change? Before/after screenshots (light + dark if relevant). -->

## Checklist

- [ ] `pnpm build && pnpm lint && pnpm test` pass locally
- [ ] UI changes verified in the dev app (`pnpm dev`)
- [ ] Visual baselines updated if the change is intentional
      (`pnpm --filter dev test:visual --update-snapshots`)
- [ ] `CHANGELOG.md` (Unreleased) updated for user-facing changes
